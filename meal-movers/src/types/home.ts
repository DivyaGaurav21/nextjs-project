import { ReactNode } from "react";

export interface ButtonLinkProps {
    href: string;
    children: ReactNode;
    className?: string;
  }
  export type contentContainerProps = {
    children: ReactNode;
    className?: string;
  }